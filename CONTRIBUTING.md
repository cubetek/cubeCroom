# المساهمة في CubeCroom

نقبل تحسينات واضحة تخدم المعلم والطالب ويمكن صيانتها على Windows وmacOS وLinux. اقرأ [AGENTS.md](AGENTS.md)؛ فهو المصدر المركزي لقواعد الهندسة والتنسيق. افصل إصلاح المشكلة عن تغييرات غير مرتبطة بها، وناقش التغيير الواسع في issue يوضح الحاجة والأثر قبل توسيع التنفيذ.

## رخصة المساهمة

المشروع تحت [AGPL-3.0-only](LICENSE) مع خيار ترخيص تجاري بديل. قبل أول مساهمة مشمولة بالاتفاقية، وقّع نموذج [Harmony CLA 1.0 المناسب](CLA.md) وأرسله إلى [info@cubetek.dev](mailto:info@cubetek.dev). يحتفظ المساهم بملكيته ويمنح حقوقاً غير حصرية؛ يتيح الخيار الخامس الترخيص التجاري مع إتاحة المساهمة أيضاً تحت رخصة المشروع عند تقديمها.

إذا كانت الحقوق لجهة عمل، استخدم نموذج الجهات بتوقيع ممثل مخوّل. لا تحتاج اتفاقية للإبلاغ عن عيب أو طرح سؤال.

## Third-party material

اذكر في طلب الدمج مصدر أي مادة منقولة ورخصتها وتعديلاتك عليها، واحتفظ بإشعاراتها الأصلية. ميّزها عن عملك؛ لا تمنح CLA حقوقاً لا تملكها في مواد طرف ثالث. راجع مخرجات أدوات AI كما تراجع أي شيفرة تقدمها. لا تودع مفاتيح المزودات أو شهادات توقيع أو بيانات طلاب فعلية؛ استخدم بيانات اختبار مصطنعة.

## إعداد البيئة

استخدم [.nvmrc](.nvmrc) وحقل `packageManager` في [package.json](package.json) لتحديد نسخ Node وpnpm. من جذر المشروع:

```sh
pnpm install --frozen-lockfile
pnpm build
pnpm dev
```

شغّل `pnpm guide:dev` للدليل. يستخدم better-sqlite3 `13.0.3` ثنائيات Node-API؛ نتحقق من تشغيلها داخل Node وElectron فعلياً. ينسخ التغليف موارد التطبيق وخادم الطلاب إلى staging مستقل، دون تغيير standalone أو ثنائيات التطوير؛ راجع [bundle-desktop.mjs](scripts/bundle-desktop.mjs). أوامر `rebuild:node` و`rebuild:electron` تتحقق من توافق الثنائي الحالي. لا تعالج مشكلة توافق بحذف بيانات المستخدم أو بتغيير ملفات الاعتماد عشوائياً.

لتحرير الدليل، عدّل `scripts/guide-copy.mjs` أو المقالات المتخصصة في `scripts/guide/*.mjs`، ثم شغّل `pnpm guide` و`pnpm guide --check`. لا تحرّر ملفات `apps/docs/content/docs` المولّدة مباشرة. ينجح التوليد من نسخة مستودع نظيفة باستخدام الصور المحفوظة؛ تحديث الصور من فحص بصري فعلي يتم عبر `pnpm guide --refresh-screens` بعد `pnpm e2e`. تتحدث اللقطات الموجودة في الفحص فقط، وتبقى اللقطات الأخرى محفوظة. راجع إزالة أي صفحة من فهرس الصور ومصدر شرحها عندما تُزال وظيفتها من التطبيق.

## قواعد التنفيذ

أصول الشعار ومصدر توليدها في [packages/ui/assets/brand](packages/ui/assets/brand/README.md). استخدم مكوّني `BrandMark` و`BrandLogo` المشتركين. عند تغيير الشعار شغّل `pnpm brand` ثم `pnpm brand --check` وأودع الأصول المولّدة معها؛ تتحقق أوامر التشغيل والبناء من تطابقها، وتشترك واجهات المعلم والطالب والموقع وأيقونات التثبيت في المصدر نفسه.

- أعد استخدام العقود المشتركة ومنطق `packages/core` و`packages/ai` بدلاً من تكراره بين واجهتي المعلم والطالب. اجعل اعتماد Electron داخل طبقة desktop وحدها.
- احفظ مسارات الملفات والأوامر محمولة بين الأنظمة. اعزل السلوك الخاص بنظام خلف واجهة واضحة، واذكر ما لم تختبره فعلياً.
- استخدم Tailwind ومكونات shadcn من `packages/ui`، والتزم بمصدر tokens الواحد المحدد في AGENTS.md. حافظ على دعم العربية وRTL والوصول بلوحة المفاتيح.
- اربط تحسين الأداء بقياس أو حالة نمو/تزامن موثقة. تجنب التجريدات المبكرة وعمليات القراءة أو التوليد المتكررة دون حاجة.
- تغييرات المخطط تحتاج مسار ترحيل وحماية بيانات يمكن مراجعتهما. لا تخفض نسخة المخطط لتجاوز فحص توافق، ولا تعتبر نسخ مجلد SQLite المفتوح بديلاً عن آلية backup القائمة.
- تعامل مع IPC والمدخلات الشبكية ومخرجات AI كبيانات تحتاج تحققاً. لا تنقل مفاتيح المزودات أو صلاحيات النشر والتوقيع إلى واجهة الطالب أو renderer.
- حدّث الوثائق والسلوك المعروض إذا تغيرت واجهة المعلم أو الطالب. يظل المعلم قادراً على فهم نتيجة عمل AI ومراجعتها.

## التحقق وطلب الدمج

شغّل الفحوص المناسبة قبل طلب المراجعة. التجميع الشامل الموجود هو:

```sh
pnpm verify
```

يشمل هذا فحص الأنواع وlint والبناء واختبارات المشروع وsmoke. يمكن تشغيل `pnpm test:lesson-editor` أو `pnpm test:ai-education` للتغييرات التي تمسها. اختبارات المزودات الحية منفصلة وقد تستعمل اعتماداً أو تكلفة؛ لا تضع مفاتيح حقيقية في PR أو سجلاته.

اختبر السلوك المتأثر واكتب اختباراً ذا معنى عند تغيير منطق أو معالجة عيب. لا تكتب اختبارات تكرر التنفيذ أو لكل تعديل توثيق أو تنسيق بسيط. عند تعديل التغليف أو OTA، فحوص المصدر لا تغني عن تركيب الحزمة وتشغيلها وتجربة تحديث نسخة مثبتة على النظام والمعمارية المعنيين. لا تُسجّل نظاماً غير متاح لك على أنه ناجح؛ اترك تحققه لمصفوفة CI أو مراجع لديه البيئة.

يجب أن يتضمن PR المشكلة والسلوك الناتج، الملفات/المنصات المتأثرة، دليل التحقق وحدوده، وأثر البيانات أو التبعيات أو الحقوق إن وجد. استخدم عنواناً بصيغة Conventional Commits مثل `fix(desktop): preserve lesson before update` أو `feat(lessons): add teacher review`. اتبع `codex/` لفروع العمل التي ينشئها Codex ما لم يطلب صاحب المشروع اسماً آخر.

تغييرات الشيفرة القادمة من forks تمر بفحوص دون أسرار إصدار. يراجع المشرفون طلبات الدمج ونتائج CI. للنشر اتبع [دليل الإصدار](scripts/release/README.md).

## المجتمع والأمان

يلزم [ميثاق السلوك](CODE_OF_CONDUCT.md) في النقاش والمراجعات. تستخدم [قناة Telegram](https://t.me/cubetek) للأسئلة العامة. للثغرات اتبع [SECURITY.md](SECURITY.md) بدلاً من نشر تفاصيل الاستغلال أو بيانات الطلاب في issue عام.
