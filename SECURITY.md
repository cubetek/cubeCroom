# سياسة الأمان

## الإصدارات المدعومة

تُطبّق الإصلاحات الأمنية على الفرع الرئيسي وأحدث إصدار مستقر عند توفره. الإصدارات الأقدم لا تتلقى إصلاحات مستقلة؛ استخدم أحدث إصدار متاح من [الإصدارات الرسمية](https://github.com/cubetek/cubeCroom/releases).

## الإبلاغ عن ثغرة

أرسل البلاغ إلى [info@cubetek.dev](mailto:info@cubetek.dev) بعنوان `CubeCroom security`. أرفق نسخة التطبيق أو commit، ونظام التشغيل، ووصف الأثر، وخطوات إعادة المشكلة. يمكن إرفاق إثبات مفهوم أو سجل منقح.

لا تنشر ثغرة غير معالجة في issue عام، ولا ترسل مفاتيح API أو بيانات طلاب أو نسخة قاعدة بيانات حقيقية. استخدم بيانات اختبار وبيئة تملكها أو لديك تصريح لاختبارها.

سنراجع البلاغ ونتواصل معك بشأن المعلومات اللازمة والإصلاح والكشف المنسق عن التفاصيل. تُنشر الإصلاحات والمعلومات المناسبة في ملاحظات الإصدار.
