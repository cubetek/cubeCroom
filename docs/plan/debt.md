# سجلّ الديون والعوائق

> **أُعيد بناء هذا الملف في ٤ سبتمبر ٢٠٢٦.** كان يعيش في `.cubeflow/PROJECT.md` واختفى مع تغيّر محتويات `.cubeflow/`. أُعيدت البنود من سجلّ العمل، وقد يكون فات منها شيء — فهو **مصدر أقلّ يقيناً من الشيفرة نفسها**. كل بند هنا يقول أين دليله في المستودع، فمن شكّ في بند فليقرأ دليله.
>
> مكانه الآن `docs/plan/` تحت سيطرة المستودع لا تحت مجلد أداة خارجية.

## كيف يُقرأ هذا الملف

| العمود | معناه |
| --- | --- |
| **البند** | الدَّين نفسه وأثره على المستخدم لا على الشيفرة |
| **الدليل** | الملف الذي يُقرأ للتحقق — لا يُصدَّق البند بلا دليله |
| **متى يُراجَع** | الشرط الذي يُغلقه، لا تاريخ متوقَّع |

---

## أ. مفتوح — يمسّ ما يراه المستخدم

| البند | الدليل | متى يُراجَع |
| --- | --- | --- |
| **ملفات خطوط IBM Plex Sans Arabic غير موجودة.** الواجهة تعمل باحتياطي `system-ui`، فهي **لا تطابق الهوية المعتمدة** في `05-foundations.md` مهما بدت مقبولة | `apps/teacher-ui/app/fonts.css` — الكتل معطّلة بتعليق | تُوضع ملفات `woff2` في `apps/teacher-ui/public/fonts/` ويُزال التعليق |
| **البندان أعلاه وأدناه مقترنان — لا يُفحص الثاني قبل الأول.** بلا `@font-face` مربوط يكون المُصيّر `system-ui`، فأيّ سلوك يُرصد اليوم صفةُ خطّ النظام لا صفةُ IBM Plex. **وزيادةً:** `apps/student-web` بلا ربط خطوط أصلاً — `globals.css` فيه لا يستورد `fonts.css`، والبند لا يذكر ذلك. | `apps/student-web/app/globals.css:1` | مع البند أعلاه |
| **تغطية `tabular-nums` للأرقام العربية `٠–٩` غير مُتحقَّق منها بصرياً.** العدّادات الحيّة (T09 · T10 · T11) قد تهتزّ مع كل تحديث | `packages/ui/src/tokens.css` — التعليق في نهاية الملف | يُفحص بصرياً في أول تشغيل حقيقي؛ البديل `min-width` على العدّادات (مطبَّق بالفعل في T10 و T11) |
| ✅ **«استعادة نسخة احتياطية» في مسار الاسترجاع** (`T01States/٣`) | بُنيت في **P6-6**: قناة تقرأ النسخ من القرص بلا قاعدة مفتوحة، وعزلٌ للبيانات القائمة بدل نسخةٍ لا تُقرأ |
| ✅ **«نقل البيانات»** — بُني: نسخٌ ثم تحويل الإشارة، **والقديم لا يُحذف** (بيانات معلمٍ لا نسخة لها عند أحد). ويرفض قبل أول بايت: مجلداً غير فارغ، أو داخل المصدر، أو حاوياً له. وكل مسار فشل يعيد فتح القديم. ~~معطّل في T22~~ نقل المجلد يحتاج إغلاق القاعدة ونقل الملفات وإعادة الفتح بلا فقد، ولا تصميم لمساره | `apps/teacher-ui/src/screens/Settings.tsx` — `disabledReason` | يُبنى مع الاستعادة في P6-3 لأنهما يتقاسمان المسار |
| ✅ **«التحقّق من التحديثات»** — أُغلق بقرار `D26`: لا يُبنى، والمفتاح الكاذب سُحب. ~~معطّل ويحتاج قرار مالك~~ التطبيق اليوم **لا يخرج إلى الإنترنت أبداً** (لا  واحد إلى الخارج). وبناؤه يفتح أول اتصال خارجي في منتجٍ مبدؤه أن لا شيء يمرّ بالإنترنت. لا يُبنى بلا إذن.، وسطر «آخر تحقّق» استُبدل بـ«لم يجرِ أي تحقّق بعد» | `apps/teacher-ui/src/screens/Settings.tsx` | P6-7 مع أول إصدار موقَّع |

| **الإعدادات المتقدّمة في T22 معدودة لا عاملة** — وأُغلق منها **تصدير سجلّات التشخيص** (زرّ في T21 يكتب ملفاً منقّىً في مجلد الصادرات). و**منفذ دخول الطلاب** (حقلٌ في T22 يكتب في إعدادات الجهاز لا في القاعدة، ويسري عند التشغيل التالي). ويبقى «إعادة الضبط» وحده — وهو موقوف على تصميم تأكيده كما نصّ البند: منفذ الطلاب · تصدير السجلّات · إعادة الضبط | `apps/teacher-ui/src/screens/Settings.tsx` | كلٌّ مع مهمّته: Phase 2 · P6-1 · وإعادة الضبط لا تُتاح قبل تصميم تأكيدها |

## ب. مفتوح — تشغيل وتغليف

| البند | الدليل | متى يُراجَع |
| --- | --- | --- |
| **تبديل `better-sqlite3` بين Electron و Node** | **تصحيح لِما قيل في P6-7:** النسختان لا تبقيان منفصلتين — pnpm يوحّدهما في الشجرة المسطّحة، فيخرج التغليف بالشجرة كلها مبنيّةً لـElectron. صار `pnpm package` يعيدها إلى Node في `finally`، فالأثر مُحتوى لا مُزال. والحلّ الجذري مجلد `node_modules` مستقل لـ`apps/desktop` — تغييرٌ في بنية المستودع لا في التغليف |
| **حزمة `standalone` تنسخ `better-sqlite3` وقت البناء** — وخادم الطلاب عملية **Node** لا Electron، فهذه النسخة تبقى على واجهة Node وهو الصحيح. لكن الترتيب يبقى مهمّاً: `rebuild:node` ⇦ `build` ⇦ التغليف | `apps/student-web/src/lib/store.ts` · `scripts/bundle-desktop.mjs` | يُفحص في أول تشغيل للمثبِّت على جهاز نظيف |
| ✅ **شحن `packages/db/migrations` مع الحزمة** | أُغلق في **P6-7**: يُشحن عبر `extraResource`، ويُقرأ من `process.resourcesPath` بعد التغليف. وفحص دخان يمنع سقوطه من الإعداد |
| **`nodeLinker: hoisted`** — شجرة مسطّحة بدل عزل pnpm | `pnpm-workspace.yaml` | ضرورة لبناء `standalone` على ويندوز بلا Developer Mode؛ يُراجَع إن انتقل البناء إلى CI لينكس فقط |

## ب-٢. مفتوح — قابلية التوسّع (مقيسة بمراجعة معمارية)

> كُشفت بمراجعة آلية للتكرار والاستعلامات. الأرقام أدناه **مقروءة من الشيفرة**، لا مقدَّرة.

| البند | الدليل | متى يُراجَع |
| --- | --- | --- |
| ✅ **`lessons.stats()` كانت تمسح جدولين كاملين بلا `WHERE`** ثم تُرشّح في JavaScript. و`lesson_reads` ينمو حاصلَ ضرب الطلاب في الدروس، والدالّة تُنادى **في مسار الطالب** مرةً لكل فتح صفحة | أُغلق: حُصر المسحان بـ`inArray` على دروس الفصل — والفهرس `lesson_reads_pk` بادئته `lesson_id` فالحصر مخدوم به. ومحروسٌ باختبار يثبت أن أثر فصلٍ آخر لا يظهر. **وقِيس:** زمن `stats` لفصلٍ صغير بجواره ٣٠٠٠٠ صفّ قراءة لفصل آخر = ٠٫٦٠× زمنِه والجارُ فارغ — أي مستقلٌّ عن جاره، وهي الخاصيّة بعينها. ولم يُثبته اختبار الحِمل: ثلاثون طالباً ببيانات قليلة لا يفرّقون بين مسحٍ كامل ومسحٍ محصور |
| **`readBy(studentId)` مسحٌ غير مفهرس** — الفهرس الوحيد `(lesson_id, student_id)`، و`student_id` ليس بادئةً صالحة. يُنادى مرةً لكل طلب طالب | `packages/db/src/repositories/lessons.ts:168` | يحتاج فهرساً جديداً ⇦ ترحيلاً ورفعَ `SCHEMA_VERSION`. مقبولٌ عند ٣٠ متزامناً (`NFR-003`)، ويلزم قبل أي وعدٍ بألف |
| ✅ **«قبول الكل» كان حلقةً بمعاملة لكل صفّ** — `for (const row of state.pending) approveRequest(row.id)`، وكلٌّ منها استعلامان ومعاملة، ثم يُعاد حساب الحالة مرتين | `apps/desktop/src/ipc.ts:686` | أُغلق: `sessions.approveMany` — قراءةٌ واحدة بـ`inArray` ومعاملةٌ واحدة للدفعة. وهي **أصحّ** لا أسرع فحسب: حلقةٌ تنقطع في منتصفها كانت تترك نصف الصفّ مقبولاً. أربعة فحوص، منها تكرار المعرّف والمعرّف المجهول |
| **مقارنة الأسماء `O(معلَّق × مقبول)`** مع تطبيعٍ عربيّ لكل مقارنة، داخل `.map` — و`requestsState` تُنادى مرتين لكل «قبول الكل» | `apps/desktop/src/ipc.ts:1665` | تافهٌ عند ٣٠، ومليونُ تطبيعٍ عند ألف |
| **نصّ الدرس المُرسَل إلى النموذج يختلف بين مسارين** — مسار المعلم (توليد الأسئلة) يتحقّق بالمخطط ويُسقط الفراغات، ومسار الطالب (اسأل عن الدرس) لا يفعل أيّاً منهما. الدرس نفسه ⇦ نصّان | `apps/desktop/src/ipc.ts:1809` مقابل `apps/desktop/src/internal.ts:154` | يُوحَّد في `contracts` حيث يعيش `lessonBlocksSchema` أصلاً |
| **ثلاث نسخ متطابقة من غلاف نداء الذكاء الاصطناعي** (مؤقّت الإلغاء، تسجيل الاستهلاك، تحويل الخطأ إلى ردّ) | `apps/desktop/src/ipc.ts` — `:459` و`:842` و`:945` | يُلَفّ في `withAiCall` محليّ. ولا يُدفع إلى `packages/ai`: مفرداتُ `status/action` شكلُ عقد IPC لا شأنُ مزوّد |
| **مكتبة تجميع الإجابات مبنيّة ومختبَرة ولا سطح لها** — قرار `D24` يمنع زرّ «درجة للعنقود»، والقيمة الباقية (ترتيب القراءة) لم تُوصَل بشاشة بعد | `packages/core/src/text/answers.ts` · ٤٨ فحصاً | قرارُ صاحب المنتج: أيُوصَل كترتيبِ قراءةٍ في `T17`، أم يُترك حتى يوجد مصحّحٌ يفهم المعنى؟ |

## ج. مفتوح — فجوات في التحقّق

| البند | الدليل | متى يُراجَع |
| --- | --- | --- |
| **التطبيق لم يُشغَّل يدوياً من طرف إلى طرف داخل نافذة Electron.** كل ما بُني مُتحقَّق منه بالترجمة و٢٣٠ اختباراً و٧٢ فحص دخان — وهي تفحص المنطق والعقود والمخرَج المبنيّ، لا تجربة المعلم أمام الشاشة | `scripts/smoke.mjs` · `packages/*/test/` | فور إعادة بناء الوحدة الأصلية: تشغيل التدفّق كاملاً (أول تشغيل ⇦ فصل ⇦ درس ⇦ حصة ⇦ طالب) |
| **تدفّق الطالب في الفحص الآليّ لا يكتمل** — الخطوات ١–٨ تمرّ (بما فيها إقلاع البوابة وظهور الرابط)، لكن النافذة الثانية التي يفتحها الفحص على رابط الطالب تبقى بلا محتوى. والخادم نفسه سليم: حالة  لا تصير «جاهز» إلا بعد ردٍّ حقيقي ٢٠٠ |  —  | حدٌّ في أداة الفحص لا في المنتج:  يتّصل بنوافذ التطبيق عند بدء الجلسة، ونافذةٌ تُنشأ بعدها لا تُقاد. البديل: قيادة صفحة الطالب من متصفّح مستقلّ (capability ثانية) بدل نافذة Electron |
| ✅ **اختبارات الواجهة** | `pnpm e2e`: سبعة سيناريوهات تقود الحزمة المغلَّفة وتنقر بالنصّ المرئي وتلتقط لقطة عند كل خطوة. ويبقى ناقصاً: تدفّق الطالب (`S01`–`S08`) ومقارنة اللقطات آلياً بين الإصدارات |
| **رمز دخول قصير يُكتب بدل الرابط** — قرار صاحب المنتج. والطالب على حاسوب يكتب `192.168.4.20:4317` بلوحة مفاتيح، وذلك أطول ممّا ينبغي وأكثر عرضةً للخطأ | `apps/desktop/src/portal.ts` · `apps/student-web/app/join/` | **يحتاج اسم مضيف قابلاً للاكتشاف (mDNS)**: الرمز وحده لا يُغني عن العنوان — الطالب يحتاج أن يصل الخادم أولاً. فالبناء: إعلان `cubecroom.local` عبر mDNS، ثم رمز من ٦ خانات يربط الحصة. لم يُبنَ بعد |
| ✅ **حِمل ٣٠ طالباً متزامنين** (NFR-003) | قِيس في P6-5: p95 = ٢٦١ms. و`pnpm load` يعيد القياس متى لزم |

| ✅ **مفتاح مساعدة النشاط في `T16`** — بُني سطحه (`D25`): المساعدة داخل النشاط تُبنى على الدرس المرتبط، والبوّابة الثالثة تُفحص **في الخادم**. وكان المفتاح يُخزَّن ويُعرض ولا يحرس شيئاً. ~~لا سطح للطالب يقرأه بعد~~ `S07` كما هو مرسوم لا يحمل لوحة مساعدة، فالبوّابة الثالثة مبنيّة وتُقرأ — لكن ما تحرسه لم يوجد بعد | `packages/core/src/student-ai.ts` · `apps/teacher-ui/src/screens/ActivityBuilder.tsx` | P5-3 مع `S07`: إمّا تُوصَل المساعدة داخل النشاط فيصير للمفتاح أثر، أو يُسجَّل أن الأنشطة بلا مساعدة قصداً |


## د. فروق الـ PRD المعلّقة

المرجع الكامل: [deltas.md](deltas.md).

| الفرق | الحالة |
| --- | --- |
| **D-1** أول تشغيل يشمل «لغة الواجهة» (FR-002) | **مخالفة معلنة**: القرار `D1` يجعل اللغة بخيار واحد، فخطوة الـwizard حُذفت وبقي الحقل في T22 |
| ✅ **D-2** AI للطالب يحتاج تفعيلاً لكل فصل ونشاط (§23) | أُغلق في **P4-7** بقرار `D10`، واكتمل مفتاحه الثالث في **P5-1** داخل `T16` |
| ✅ **D-3** الأرشفة في T06/T07 | أُغلق في P3-1 بقرار `D8` |
| ✅ **D-4** مفتاح AI لا يُصدَّر إلى النسخة الاحتياطية (§16) | أُغلق في **P4-1**: المفتاح يعيش في `userData` والأرشيف يُبنى من القاعدة ومجلد `files` بالاسم، فلا يدخل بحكم البنية. اختبارٌ يضع سرّاً بجوار القاعدة ويثبت أنه لا يظهر في أي بايت من الأرشيف. يبقى **سطر في لوح T20** يقول للمعلم إنه سيعيد إدخال مفتاحه بعد الاستعادة |
| **D-5** ٣٠ اتصالاً متزامناً | بند اختبار حِمل — P6-5 |
| **D-6** مطابقة نصوص الحالة (§17) | مطابقة نصّية عند بناء `T21` — P6-1 |

## هـ. مغلق — يُحفظ ليُعرف أنه عولج

| البند | كيف أُغلق |
| --- | --- |
| لا سياسة لأنواع الملفات المقبولة | **P3-5**: المرفق يُقدَّم بـ `Content-Disposition: attachment` ونوع محايد، فلا يُنفَّذ `.svg` ولا `.html` في متصفّح الطالب. الحلّ في موضعه المقرَّر: التقديم لا حظر الرفع — ومحروس بفحص دخان |
| محرّك بوابة الطالب لا يناديه شيء في الواجهة | **P2-2**: وُصل بلوح T09 داخل قشرة الفصل |
| عدّادا T09 غائبان | **P2-5** ثم **P2-6**: صارا يقيسان الحضور من نبضة صفحة الطالب لا من قبولٍ سابق |
| بيانات عيّنة ثابتة في الرئيسية | **P2-7**: حُذفت، وكل رقم على الشاشة يأتي من القاعدة أو من الخادم |
| **`/join` كان يعرض نصّاً مؤقّتاً من P1** — شاشة الانضمام (`JoinFlow`) بُنيت في P2-3 ولم تُوصَل بمسارها قطّ. ولم يكشفه شيء: فحص الدخان كان يسأل «هل يردّ ٢٠٠؟» والصفحة تردّ به بأمانة | **وُصلت الشاشة بمسارها** (`app/join/page.tsx`، `force-dynamic` لأنها تقرأ الحصة من القاعدة)، وصار فحص الدخان يسأل عمّا **يراه الطالب**: حقل الرمز، وحقل الاسم، وزرّ الإرسال، واسم الفصل — وأن لا أثر لنصّ مرحلة سابقة |
| **صفحة الطالب تُحمَّل ثم تُمحى**: `script-src 'self'` تمنع سكربتات Next المضمَّنة، فيسقط الترطيب ويرى الطالب **بياضاً** بلا رسالة ولا سجلّ | **السياسة انتقلت إلى `middleware.ts`** برقم استعمال (nonce) يتغيّر مع كل طلب و`strict-dynamic`. وهو العطل نفسه الذي أصاب واجهة المعلم سابقاً وعولج هناك بالبصمات — ولم يُبحث عنه هنا لأن لا فحص كان يُحمّل الصفحة في متصفّح |
| **الحزمة تُشحن بواجهة معلم قديمة**: `turbo.json` لم يُعلن `out-next/**` مخرَجاً للبناء، فإصابةُ الذاكرة المؤقتة تترك على القرص ما بناه أمس | أُضيف `out-next/**` إلى `outputs`. وأُصلح معه `CUBECROOM_OUT_DIR` النسبيّ: كان يهبط في جذر المستودع بينما الفحص البصريّ يبحث عنه بجانب التطبيق — فيُفحص بناءٌ قديم ويُظنّ الجديد سليماً |
| **الطالب يبقى ينظر إلى دروسٍ لم يعد يملكها** بعد إنهاء الحصة: الاستطلاع يفشل في الشبكة (لا ٤٠٣)، والاستثناء كان يُبتلع صامتاً — فشاشة `S09` المرسومة لم تكن تظهر أبداً في التدفّق الحقيقي | **D18**: شاشة «انقطع الاتصال بجهاز معلمك» بعد ثلاث محاولات فاشلة، على الرئيسية وعلى شاشة الانتظار — بلا جزمٍ بالسبب. ومحروسة بسيناريو e2e يُنهي الحصة فعلاً ثم ينتظر ظهورها |
| **صفحاتٌ لا تستطلع تبقى جامدة بعد انقطاع الاتصال** — عارض الدرس والنشاط والإيصال؛ الطالب عليها لا يعلم أن الحصة انتهت حتى يتنقّل فيرى خطأ المتصفّح | **D19**: نبضة `‎/api/student/ping` كل ثلاث ثوانٍ (تسع ثوانٍ للاكتشاف) على تلك الصفحات، فتُبدَّل الشاشة قبل أن ينقر. و**Service Worker غير متاح**: أصل الطالب ليس سياقاً آمناً — مقيسٌ بالتجربة لا مفترضاً. محروسٌ بسيناريو يُبقي الطالب على عارض الدرس ثم يُنهي الحصة، وبآخر يبدأ حصةً جديدة فيرى الطالب «انتهت الحصة» جزماً |
| **حفظ المعلم كان يمحو إجابات الطلاب** — أيّ حفظٍ لنشاطٍ منشور يُفرّغ `answers.option_id` لكل طالب، بصمت، وإيصالُ الطالب يقول إن إجابته محفوظة. لم يكشفه شيء: كل الفحوص كانت تحفظ **قبل** التسليم لا بعده | **D23**: التوفيق بدل الاستبدال. أُثبت بإعادة إنتاج قبل الإصلاح وبعده، ويحرسه `answer-safety.test.mjs`. وكُشف عرَضاً أثناء بناء «تجميع أخطاء الاختيار» — لا ببحثٍ عنه |
| فحوص تسقط عشوائياً بـ `ENOTEMPTY` على ويندوز | **P1-5**: إعادة محاولة الحذف في تنظيف الاختبارات، مع إثبات الاستقرار بثماني دورات |
