/**
 * تحويل الأرقام إلى عربية `٠–٩` — قرار D6 في docs/design/06-decisions.md.
 *
 * تُستعمل لكل ما يراه المستخدم: الأعداد، الأوقات، التواريخ، العدّادات.
 * ولا تُستعمل للقيم التقنية (رابط الدخول · IP/Port · أسماء النماذج · المفتاح
 * المُقنَّع) — تلك تبقى لاتينية داخل عنصر يحمل الصنف `ltr-island`.
 */
export { ar } from '@cubecroom/contracts';

/*
 * الاتجاه المعاكس يُعاد تصديره من `contracts` — الأصل الواحد هناك.
 * ويبقى مُصدَّراً من هنا لأن مكوّنات الواجهة تستورده بهذا الاسم منذ البداية،
 * وتغييرُ مسار استيرادٍ في كل مستهلك تكلفةٌ بلا مقابل.
 */
export { toLatinDigits } from '@cubecroom/contracts';

/**
 * حجم الملف كما يعرضه لوح T18: «٨٤٠ ك.ب» · «١٫٤ م.ب» · «٣١٤ م.ب».
 *
 * الفاصلة العربية `٫` لا النقطة — وهي فاصلة عشرية حقيقية هنا لا فاصل أجزاء
 * كما في رقم الإصدار. وخانة عشرية واحدة تحت المئة وصفرٌ فوقها: «٣١٤٫٢ م.ب»
 * دقّةٌ لا يستعملها أحد وتُطيل الصفّ.
 */
export { formatBytes } from '@cubecroom/contracts';
